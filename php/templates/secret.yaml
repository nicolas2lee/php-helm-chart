{{- if hasKey .Values "passwords"}}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}"
  namespace: {{ .Release.Namespace | quote }}
  labels:
        {{- include "php.labels" . | nindent 4 }}
data:
  {{ toYaml .Values.passwords | indent 2}}
{{- end}}